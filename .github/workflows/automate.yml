name: Automated Video Generator

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  generate_videos:
    # Linux (ubuntu-latest) पर FFmpeg पहले से स्थापित होता है, जो OpenCV और subprocess के लिए ज़रूरी है।
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      - name: 🛠️ Setup Python and Dependencies
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      # यह स्टेप सभी ज़रूरी Python लाइब्रेरीज़ को इंस्टॉल करेगा।
      - name: 📦 Install Python Libraries
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: ⚙️ Run Video Automation Script
        run: |
          # NOTE: Use your actual Google Sheet's export-as-CSV link
          python automation.py "https://docs.google.com/spreadsheets/d/1kJWflnWA6BpZqyndGu4zqKXL2rlTRYBLGVOjOjXrXt0/export?format=csv"
